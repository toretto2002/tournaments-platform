uv lock                 # rigenera uv.lock con aiokafka 0.14.* e le sue dipendenze
uv export --no-hashes --no-dev --format requirements.txt -o requirements.txt   # i Dockerfile usano requirements.txt, non pyproject.toml/uv.lock — vanno rigenerati o il build userà ancora 0.12
